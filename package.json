{
    "name": "fsm-vir",
    "version": "1.0.0",
    "description": "The heroic finite state machine package.",
    "keywords": [
        "fsm",
        "finite state machine",
        "state machine",
        "node",
        "nodejs",
        "js",
        "javascript",
        "typescript",
        "ts",
        "vir"
    ],
    "homepage": "https://github.com/electrovir/fsm-vir#readme",
    "bugs": {
        "url": "https://github.com/electrovir/fsm-vir/issues"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/electrovir/fsm-vir.git"
    },
    "license": "MIT",
    "author": "electrovir (github.com/electrovir)",
    "main": "dist/index.js",
    "scripts": {
        "prepublishOnly": "rm -rf dist && npm install && npm run test && npm run spellcheck",
        "test": "tsc && test-vir \"./**/!(*.type).test.js\"",
        "spellcheck": "cspell --no-progress --unique \"./**/*\"",
        "test:full": "npm run test && npm install && npm run spellcheck -- --no-progress && npm run format:check",
        "format": "npm run format:base -- --write",
        "format:base": "prettier --ignore-path .gitignore \"./{,!(dist)/**/}*.+(ts|json|html|css|md)\"",
        "format:check": "npm run format:base -- --check"
    },
    "devDependencies": {
        "@types/node": "^16.4.3",
        "cspell": "^5.6.6",
        "prettier": "^2.3.2",
        "prettier-plugin-jsdoc": "^0.3.23",
        "prettier-plugin-organize-imports": "^2.3.3",
        "prettier-plugin-packagejson": "^2.2.11",
        "prettier-plugin-sort-json": "^0.0.2",
        "test-vir": "^0.2.0",
        "typescript": "^4.3.5"
    }
}
